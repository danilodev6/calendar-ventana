# Reservas Casa — ejemplo de configuración local (Fase 0).
# En esta fase no hay variables requeridas; la URL de SQLite se agregará en la Fase 3.
# Copiar a .env solo cuando una fase lo indique. Nunca commitear el .env real.
